---
// /contestants -> most recent directory year.
// Bump this when a new competition year comes online so the bare
// URL always lands on the current cycle.
return Astro.redirect("/contestants/2026");
---
