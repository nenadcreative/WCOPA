---
const { classes, size = "medium" } = Astro.props;

const sizes = {
  small: "max-w-3.5xl",
  medium: "max-w-7xl",
  large: "max-w-7xl",
};
---

<div class=` mx-auto w-full px-2 xl:px-4 ${sizes[size]} ${classes} `>
  <slot />
</div>
